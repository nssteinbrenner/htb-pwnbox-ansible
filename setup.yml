---
- name: "Set up pwnbox hosts"
  hosts: "{{ target if target is defined else 'pwnbox_hosts' }}"
  become: true

  roles:
    - packages
    - repositories
    - misctools
    - hackthebox
    - system
